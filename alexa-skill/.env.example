# Copy to .env (gitignored). Same API host as the Flutter app.

# Kitzz Go server — no trailing slash.
# Local:   http://127.0.0.1:8080
# ngrok:   https://YOUR-ID.ngrok-free.app  (same URL as Flutter dart-define)
BACKEND_URL=http://127.0.0.1:8080

# Local Express skill server (see backend ALEXA_SKILL_URL)
SKILL_PORT=3000
